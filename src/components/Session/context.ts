import React from 'react';

// @Jon/@Crista: Should this be called ClowdrContext or something?
//Jon: Yes, that would be a good name
const AuthUserContext = React.createContext(null);

export default AuthUserContext;
