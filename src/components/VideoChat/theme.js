import { createMuiTheme } from '@material-ui/core';


export default createMuiTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#F22F46',
        },
    },
    sidebarWidth: 260,
    sidebarMobileHeight: 90,
});
